# Базовый URL backend API. По умолчанию "/api" — работает в Docker, где nginx
# проксирует /api на Laravel. Для локальной разработки без Docker (frontend
# на :5173, backend на :8000) укажите абсолютный адрес:
# VITE_API_URL=http://localhost:8000/api
